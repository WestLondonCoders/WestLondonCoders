$('#comments-list').html('<%= escape_javascript(render partial: "/posts/comment_feed", locals: { post: @post, comments: @post.comments.published.most_recent_first }) %>');
$('.js-remote-text-field').val('');
setRevealTriggers();
