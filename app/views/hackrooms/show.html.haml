- set_page_title(@hackroom.name)
.m-profile
  = render partial: 'sidebar'

  .m-profile__content
    = render partial: 'hackroom_nav', locals: { selected: 'projects' }

    .m-profile__current-content
      .m-profile__page
        %p.u-center This hackroom hasn't shared their projects yet.
