$('.is-selected').removeClass('is-selected')
$('#hackroom-nav').addClass('is-selected')
$("#profile-content").html("<%= escape_javascript(render partial: 'shared/hackroom_cards', locals: { hackrooms: @user.all_hackrooms } ) %>");
