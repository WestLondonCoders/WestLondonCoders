$('.is-selected').removeClass('is-selected')
$('#comment-nav').addClass('is-selected')
$("#profile-content").html("<%= escape_javascript(render partial: 'users/discussion', locals: { commentable: @user } ) %>");
