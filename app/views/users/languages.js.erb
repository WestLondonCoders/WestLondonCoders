$('.is-selected').removeClass('is-selected')
$('#language-nav').addClass('is-selected')
$("#profile-content").html("<%= escape_javascript(render partial: 'hackrooms/language_list', locals: { user: @user } ) %>");
