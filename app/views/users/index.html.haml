- set_page_title("Members")

%section.a-section
  .a-content
    %h1.u-center
      #user-search-heading
        = render 'heading'

    = render partial: 'filter'
    = render partial: 'sort'

    #user-search-results
      = render 'results'
