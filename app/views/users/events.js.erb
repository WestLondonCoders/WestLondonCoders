$('.is-selected').removeClass('is-selected')
$('#meetup-nav').addClass('is-selected')
$("#profile-content").html("<%= escape_javascript(render partial: 'shared/events', locals: { events: @user.events.upcoming } ) %>");
