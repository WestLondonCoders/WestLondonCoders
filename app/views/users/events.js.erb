$('.is-selected').removeClass('is-selected')
$('#meetup-nav').addClass('is-selected')
$("#profile-content").html("<%= escape_javascript(render partial: 'events/event_list', locals: { events: @user.events.upcoming } ) %>");
