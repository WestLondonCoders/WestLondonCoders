$('.is-selected').removeClass('is-selected')
$('#comment-nav').addClass('is-selected')
$("#language-content").html("<%= escape_javascript(render partial: 'shared/comments', locals: { commentable: @language } ) %>");
