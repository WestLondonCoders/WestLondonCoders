- if language.primary_hackrooms.any? || language.hackrooms.any?

  - if language.primary_hackrooms.any?
    %h2
      = pluralize(language.primary_hackrooms.count, 'hackroom')
      primarily using
      = language.name

    = render partial: 'shared/hackroom_list', locals: { source: language.primary_hackrooms }

  - if language.hackrooms.any?
    %h2
      = pluralize(language.hackrooms.count, 'hackroom')
      involving some
      = language.name

    = render partial: 'shared/hackroom_list', locals: { source: language.hackrooms }

- else
  %p
    There are no hackrooms using
    = language.name
    yet.
    Want to
    = succeed '?' do
      = link_to "start one", new_hackroom_path
