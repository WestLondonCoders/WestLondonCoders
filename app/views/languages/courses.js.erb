$('.is-selected').removeClass('is-selected')
$('#courses-nav').addClass('is-selected')
$("#language-content").html("<%= escape_javascript(render partial: 'shared/courses', locals: { courses: @language.courses.published } ) %>");
