- set_page_title("Interests")

%section.a-section
  .a-content
    = render partial: 'shared/people_tabs'
    %h1.a-heading
      #interest-search-heading
        = render 'heading'

    = render partial: 'filter_interests'
    = render partial: 'sort_interests'

    #interest-search-results
      = render 'results'
