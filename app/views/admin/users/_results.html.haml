%table.m-admin-table.u-margin-top
  %thead
    %tr
      %th{style: 'width: 20%'}
        Name
      %th{style: 'width: 5%'}
        %i.fa.fa-bell{title: 'Unread notifications'}
      %th.u-center{style: 'width: 10%'}
        %strong Hackrooms
      %th{style: 'width: 45%'}
        %strong Roles
      %th{style: 'width: 5%'}

  %tbody
    - @users.each do |user|
      %tr
        = form_for user, url: admin_user_path(user) do |f|
          %td{style: 'width: 20%'}
            = user.name
          %td{style: 'width: 5%'}
            = user.notifications.unread.count
          %td.u-center{style: 'width: 10%'}
            = (user.hackrooms.count + user.own_hackrooms.count)
          %td{style: 'width: 45%'}
            .m-form__checkboxes
              = f.collection_check_boxes :role_ids, Role.all, :id, :name do |b|
                .m-form__checkbox
                  = b.check_box
                  = b.label
          %td{style: 'width: 5%'}
            .m-admin-table-submit
              = f.submit 'Update', class: 'a-button'
