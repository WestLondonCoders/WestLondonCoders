.a-filter
  = search_form_for @search, url: search_meetups_path, method: :post, remote: true do |f|
    = f.search_field :name_cont, id: 'search', placeholder: 'Search'
    = f.submit "Search", class: 'a-button m-filter__button'
